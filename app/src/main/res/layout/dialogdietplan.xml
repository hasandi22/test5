<!-- dialog_diet_plan.xml -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="@drawable/rounded_fd">

    <TextView
        android:id="@+id/tvDietPlanTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Diet Plan for Monday"
        android:textSize="18sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/tvDietPlanDetails"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Details of the nutrition plan for the day."
        android:textSize="16sp" />

    <Button
        android:id="@+id/btnClose"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Close"
        android:backgroundTint="@android:color/holo_red_light"
        android:textColor="@color/white"
        android:layout_marginTop="12dp"/>
</LinearLayout>
